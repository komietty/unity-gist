using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoroutineUtil : MonoBehaviour
{
   // public IEnumerator SquareWaveRepeater(Vector2 stayDuration, Vector2 peakDuration, out float value, float min = 0, float max = 1) {
   //     yield return null;
   //     while (true) {
   //         float stay = Random.Range(stayDuration.x, stayDuration.y);
   //         float peak = Random.Range(peakDuration.x, peakDuration.y);

   //         yield return new WaitForSeconds(stay);

   //     }
   // }
}
